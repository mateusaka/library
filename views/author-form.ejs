<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <title>Library</title>
</head>

<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-2">
                <%- include("./partials/sidebar.ejs")%>
            </div>

            <div class="col-sm-10">
                <h1>
                    <%=title%>
                </h1>

                <form method="POST" action="">
                    <div class="form-group">
                        <label class="mb-1" for="firstName">First name:</label>
                        <input id="firstName" class="form-control mb-3" type="text" placeholder="First name" name="firstName" required="true" value="<%=(typeof author === 'undefined' ? '' : author.firstName)%>" />
                        <label class="mb-1" for="lastName">Last name:</label>
                        <input id="lastName" class="form-control mb-3" type="text" placeholder="Last name" name="lastName" required="true" value="<%=(typeof author === 'undefined' ? '' : author.lastName)%>" />
                    </div>
                    
                    <div class="form-group">
                        <label class="mb-1" for="dateOfBirth">Date of birth:</label>
                        <input id="dateOfBirth" class="form-control mb-3" type="date" name="dateOfBirth" placeholder="dd/mm/yyyy" value="<%=(typeof author === 'undefined' ? '' : author.dateOfBirthFormattedYYYYMMDD)%>" />
                    </div>

                    <div class="form-group">
                        <label class="mb-1" for="dateOfBirth">Date of death:</label>
                        <input id="dateOfDeath" class="form-control mb-3" type="date" name="dateOfDeath" placeholder="dd/mm/yyyy" value="<%=(typeof author === 'undefined' ? '' : author.dateOfDeathFormattedYYYYMMDD)%>" />
                    </div>
                    
                    <button class="btn btn-primary mb-3" type="submit">Submit</button>
                </form>

                <% if (locals.errors) {%>
                    <ul>
                        <% for(let error of errors) {%>
                            <li><%=error.msg%></li>
                        <%}%>
                    </ul>
                <%}%>
            </div>
        </div>
    </div>
</body>

</html>